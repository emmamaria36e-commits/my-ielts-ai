# AGENTS.md

本文件定义 AI Agent 在 My IELTS AI 仓库中的长期协作规则，适用于整个仓库。

## 1. Scope and Sources of Truth

- 用户当前请求和明确约束优先于本文件中的默认工作方式。
- 开始任务前，先阅读与任务直接相关的代码和项目文档。
- 产品范围以 `docs/PRODUCT.md` 为准，开发顺序以 `docs/ROADMAP.md` 为准。
- 当前系统结构以 `docs/ARCHITECTURE.md` 为准，技术选择及其理由以 `docs/DECISION_LOG.md` 为准。
- 如果文档与代码不一致，应指出差异，不擅自假定其中一方正确。

## 2. Before Making Changes

- 修改前检查 Git 状态，识别已有的修改和未跟踪文件。
- 用户已有的工作一律保留；不覆盖、清理或回退来源不明的改动。
- 用户要求只读分析、方案或报告时，不修改、暂存或提交任何文件。
- 涉及架构、依赖、安全边界、数据格式或多个模块的实质性修改时，先输出方案并等待确认。
- 如果任务缺少会显著改变结果的产品或技术决策，应先说明选项、影响和推荐方案。

## 3. Implementation Principles

- 只解决当前已确认的任务，保持修改范围最小。
- 优先采用满足当前需求的简单方案，避免过度设计。
- 不为尚未确认的未来功能提前实现抽象、配置或扩展点。
- 不顺手重构无关模块，不进行无关的批量格式化或换行符转换。
- 除非任务明确要求改变，否则保持现有接口和用户行为兼容。
- 发现范围外的问题时，先报告并建议记录，不擅自扩大实现范围。

## 4. Product Focus

- 当前开发优先保证已经确认的 MVP 核心流程。
- 不主动实现、扩展或预留未进入当前范围的功能，除非用户明确要求。
- 实现新功能前，应评估其是否支持当前产品目标、是否影响核心流程，以及是否引入不必要的维护成本。
- 涉及产品范围变化时，先提出方案并等待确认。

## 5. Security and Data Handling

- 密钥、令牌和服务凭据不得进入客户端代码、Git、日志或项目文档。
- AI 生成内容和外部服务返回结果均视为不可信数据。
- AI 生成内容必须经过格式、结构和业务规则验证，才能进入展示、存储或后续处理流程。
- 不直接信任模型返回的 HTML、JSON 或其他结构化数据；JSON 必须解析并验证预期结构。
- 展示层默认以纯文本方式处理模型内容，不得直接渲染模型生成的 HTML。
- 如果业务确实需要展示 HTML，必须使用明确的标签白名单进行清理，并说明必要性。
- 错误信息和调试输出不得泄露凭据、内部配置或其他敏感数据。

## 6. Git and Commit Safety

- 未经用户明确授权，不创建或切换分支，不暂存、提交、打标签、推送或创建 Pull Request。
- Commit 应保持原子性，只包含一个清晰目的。
- 不把业务修改、无关重构、批量格式化和文档整理混入同一个 Commit。
- Commit 前检查最终 Diff，确认没有意外文件或敏感信息。
- 不使用 `git reset --hard`、强制覆盖等破坏性操作处理用户工作。

## 7. Validation

- 根据修改风险执行相称的语法检查、自动化测试、构建检查或手动主流程验证。
- 优先验证用户可见行为和被修改模块的边界条件。
- 明确区分已验证、未验证和因外部条件无法验证的内容。
- 外部服务、密钥或环境未配置时，不得声称端到端流程已经通过。
- 验证失败时先调查原因，不通过弱化测试或隐藏错误来制造成功结果。

## 8. Documentation

- 长期文档只记录具有持续价值的事实、决策、里程碑和复盘，不记录普通修改流水账。
- 同一事实只在一份文档中完整描述，其他文档通过编号或链接引用。
- 系统当前结构发生变化时，更新 `docs/ARCHITECTURE.md`。
- 产品范围或开发顺序发生变化时，更新 `docs/PRODUCT.md` 或 `docs/ROADMAP.md`。
- 形成长期技术决策时，更新 `docs/DECISION_LOG.md`。
- 完成重要里程碑时，更新 `docs/DEVELOPMENT_LOG.md`。
- 修复严重、回归或根因复杂的问题时，更新 `docs/BUG_NOTES.md`。
- AI 参与重要审查、迁移或开发方法改进时，更新 `docs/AI_COLLABORATION.md`。
- 普通样式、文案和简单代码修改不更新长期文档，除非用户明确要求。
- 需要记录但对应文档尚不存在时，先说明建议，不在无关任务中擅自扩展文档体系。

## 9. Completion Report

完成修改后，向用户说明：

- 修改了哪些文件。
- 为什么进行这些修改。
- 使用什么方式验证，结果如何。
- 哪些内容尚未验证以及原因。
- 是否更新了长期文档；如果没有，说明该任务未达到记录条件。
- 是否存在遗留风险或需要用户决定的下一步。
